services:
  - type: web
    name: log-file-anomaly-explainer
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run Project/app.py --server.port $PORT --server.address 0.0.0.0
